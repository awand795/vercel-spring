{
  "name": "vercel-spring",
  "version": "0.1.3",
  "description": "Spring runtime for Vercel - deploy Spring Boot apps using GraalVM Native Image",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/launcher.mjs"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "jest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "tar": "^7.4.0"
  },
  "peerDependencies": {
    "@vercel/build-utils": ">=7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/build-utils": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "keywords": [
    "vercel",
    "spring-boot",
    "java",
    "graalvm",
    "native-image",
    "runtime",
    "serverless"
  ],
  "license": "MIT"
}
